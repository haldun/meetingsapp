<%= simple_form_for(room) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :topic %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
